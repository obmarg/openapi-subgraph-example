federation_version: =2.3.2
subgraphs:
  one:
    schema:
      subgraph_url: http://localhost:8091/graphql
      introspection_headers:  # Optional headers to include in introspection request
        x-api-key: "whatever"
  two:
    schema:
      subgraph_url: http://localhost:8090/graphql
      introspection_headers:  # Optional headers to include in introspection request
        x-api-key: "whatever"
